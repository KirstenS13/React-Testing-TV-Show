import React from 'react';
import {render} from '@testing-library/react';
import Episodes from './Episodes';

test('rerenders correctly with the list of episodes', () => {
    // Arrange
        // render Episodes component and set up mock data
        // make sure component is empty on initial render

    // Act
        // rerender Episodes component with the mock episodes data

    // Assert
        // make sure episodes are rendered to page correctly
        // number of episodes should match length of mock data
});